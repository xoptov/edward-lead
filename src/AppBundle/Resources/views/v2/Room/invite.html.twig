{% extends '@App/v2/layout.html.twig' %}

{% block content %}
    <div class="content">
        <div class="row">
            <div class="col-12 content_box">
                <div class="content_title">
                    <div class="main_title">
                        <h1>Пригласить компанию в комнату #{{ room.id }} - {{ room.name }}</h1>
                    </div>
                </div>
                <div class="row">
                    <div class="col-xl-5 invite_webmaster">
                        <div class="web_title">Отправьте ссылку</div>
                        <div class="box_link">
                            <div class="link">
                                <img src="{{ asset('bundles/app/v2/img/link.png') }}">
                            </div>
                            <div class="text">
                                {{ absolute_url(path('app_room_invite_confirm', {'token': room.inviteToken})) }}
                            </div>
                        </div>
                        <p>Вы можете отправить партнеру прямую ссылку, чтобы пригласить присоедениться его к своей комнате</p>
                    </div>
                    <div class="col-xl-7 invite_webmaster">
                        <div class="web_title">
                            Отправьте приглашение на эл. почту
                        </div>
                        <div class="form">
                            <form id="invite">
                                <div class="fields">
                                    <input name="email" id="email" placeholder="Введите E-mail" type="text">
                                </div>
                                <button type="submit">Отправить</button>
                                <input type="hidden" id="token" value="{{ room.inviteToken }}">
                            </form>
                        </div>
                        <p>Партнёру придёт письмо с приглашением присоедениться <br> к закрытой комнате</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
